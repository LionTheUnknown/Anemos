services:
  postgres:
    image: postgres:16-alpine
    container_name: anemos-db
    environment:
      POSTGRES_DB: anemos
      POSTGRES_USER: anemos
      POSTGRES_PASSWORD: anemos
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
